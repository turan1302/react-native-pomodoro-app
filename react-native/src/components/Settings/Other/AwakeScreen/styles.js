import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container : {
        backgroundColor: "rgba(128, 128, 128, 0.3)",
        marginTop: 15,
        justifyContent: "center",
        alignItems: "center",
        padding: 10
    },

    buttons_area : {flexDirection: "row"},
    right_button : {marginLeft: 50},
    button_title : {fontWeight: "bold", color: "#fff", fontSize: 15, textAlign: "center", marginTop: 5}
});

export default styles;
